services:
  - type: web
    name: truthlens-api
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /
    envVars:
      - key: VID_ONNX_URL
        value: "https://drive.google.com/uc?id=REPLACE_WITH_YOUR_DRIVE_FILE_ID"
