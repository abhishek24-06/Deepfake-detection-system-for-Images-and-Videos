flask
flask-cors
gunicorn

# PyTorch CPU installed separately in build command (to avoid CUDA variant)
# torch
# torchvision

# TensorFlow CPU installed separately in build command
# tensorflow-cpu

# Computer vision
opencv-python-headless
numpy
Pillow

# Google Drive model download (video model)
gdown