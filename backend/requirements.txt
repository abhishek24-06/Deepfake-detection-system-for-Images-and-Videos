flask
flask-cors
gunicorn

# ONNX Runtime — runs both models with ~80MB RAM (replaces PyTorch + TensorFlow)
onnxruntime

# Computer vision
opencv-python-headless
numpy
Pillow

# Google Drive model download (video ONNX model)
gdown